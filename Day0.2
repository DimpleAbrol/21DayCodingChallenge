
    boolean isCircular(Node head)
    {
        if(head == null){
            return true;
        }
        Node temp = head;
        do{
            temp = temp.next;
        }while(temp!=null && temp!=head);
        return(temp == head);

}
